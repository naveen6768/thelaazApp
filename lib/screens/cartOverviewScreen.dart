import 'package:flutter/material.dart';

class CartOverviewScreen extends StatelessWidget {
  static const id = 'CartOverviewScreen';
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Cart Screen'),
    );
  }
}
