import 'package:flutter/material.dart';

class MenuPlanScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('MenuPlan'),
    );
  }
}
